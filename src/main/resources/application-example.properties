# Example configuration - rename/copy to application.properties and fill in real values

spring.application.name=Property-App

# Database Configuration (replace with your actual values)
spring.datasource.url=jdbc:mysql://localhost:3306/YOUR_DATABASE_NAME
spring.datasource.username=YOUR_DB_USERNAME
spring.datasource.password=YOUR_DB_PASSWORD

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (Optional)
server.port=8080

# =============================================
# INSTRUCTIONS:
# 1. Copy this file to application.properties
# 2. Fill in your actual database credentials
# 3. NEVER commit application.properties to version control!
# =============================================